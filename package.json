{
  "name": "~TODO~",
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm run sass & nodemon --watch parse.js parse.js & svelte-kit dev",
    "build": "svelte-kit build",
    "sass": "node-sass src/app.sass static/app.css -w",
    "preview": "svelte-kit preview"
  },
  "devDependencies": {
    "@sveltejs/kit": "next",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "markdown-it": "^12.0.6"
  }
}